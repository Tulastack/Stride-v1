PORT=3000

# Aurora DSQL connection — password is auto-generated via IAM
DB_HOST=<your-cluster-endpoint>.dsql.us-east-1.on.aws
DB_PORT=5432
DB_NAME=postgres
DB_USER=admin

# AWS credentials (or use aws configure / instance profile)
AWS_REGION=us-east-1
AWS_PROFILE=default
