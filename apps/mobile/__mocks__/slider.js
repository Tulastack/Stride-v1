const React = require('react');
const { View } = require('react-native');
function Slider(props) { return React.createElement(View, props); }
module.exports = Slider;
module.exports.default = Slider;
