// Re-export of the canonical design tokens for app-side ergonomics.
// The single source of truth lives in @stride/design-tokens (PROMPT F.0).
export {
  primitive,
  semantic,
  typography,
  spacing,
  radius,
  borderWidth,
  tokens,
} from '@stride/design-tokens';
export type { Tokens } from '@stride/design-tokens';
